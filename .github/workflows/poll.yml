name: Poll ianseo results

on:
  schedule:
    # every 15 minutes, offset from the herd minute marks
    - cron: "9-54/15 * * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  poll:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Fetch and parse ianseo results
        run: python poller/poll.py

      - name: Fetch and parse HDH-IAA results
        run: python poller/hdhiaa_poll.py

      - name: Commit updated results
        run: |
          git config user.name "ianseolive-bot"
          git config user.email "ianseolive-bot@users.noreply.github.com"
          git add data/results.json data/hdhiaa.json index.html hdhiaa/index.html
          if git diff --cached --quiet; then
            echo "no changes"
          else
            git commit -m "Update results $(date -u +%Y-%m-%dT%H:%M:%SZ)"
            git push
          fi
